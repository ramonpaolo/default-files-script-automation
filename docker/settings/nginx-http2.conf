upstream node {
    least_conn;

    server nodejs:3000;
}

server {
    listen 80;
    listen 443 ssl;

    ssl_certificate 'server.crt';
    ssl_certificate_key 'server.key';

    location / {
        proxy_pass https://node;
    }
}